import 'package:flutter/material.dart';

class AddVehicleManuallyScreen extends StatelessWidget {
  const AddVehicleManuallyScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
